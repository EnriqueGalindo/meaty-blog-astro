# gitleaks config (MEAT-9). Extends the built-in ruleset rather than replacing it.
[extend]
useDefault = true

[allowlist]
description = "Committed placeholder env file holds empty values only."
paths = [
  '''\.env\.example$''',
]
