# build output
dist/
# generated types
.astro/
# firebase deploy cache (MEAT-33)
.firebase/

# content synced from GCS at build time (MEAT-41): markdown + co-located images
# are materialized locally by scripts/sync-content.mjs and never committed.
src/content/posts/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*


# environment variables — never commit real secrets (see MEAT-9)
# Ignore .env AND every .env.* variant (.env.local, .env.production, ...),
# but keep the committed placeholder .env.example.
.env
.env.*
!.env.example

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Local cache of the Astro reference docs (sparse clone of withastro/docs).
# On-disk reference only — never committed. See CLAUDE.md "Astro docs cache".
.astro-docs/
